export enum States {
  empty = 0,
  asc = 1,
  dsc = -1,
}

export enum NextStates {
  empty = 1,
  asc = -1,
  dsc = 0,
}

export enum Icons {
  countView = 'countView',
  countPlay = 'countPlay',
  countSend = 'countSend',
  countOrder = 'countOrder',
  countPay = 'countPay',
  viewSend = 'viewSend',
  viewPay = 'viewPay',
}
